'use strict';
app.controller('homeController', ['$scope', '$rootScope', '$filter', 'homeService', 'localStorageService', '$location', function ($scope, $rootScope, $filter, homeService, localStorageService, $location) {
    debugger;
    try {
        $scope.emsg = "start";


    }
    catch (e) { }
}]);